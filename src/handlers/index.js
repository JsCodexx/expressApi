const { EmployeeAttendance } = require("./employeeAttendance");
const { Login, updatePassword } = require("./login");
const { SignupEmployee } = require("./signupEmployee");
const { EmployeeManagement } = require("./employeeManagement");

module.exports = {
  EmployeeAttendance,
  Login,
  updatePassword,
  SignupEmployee,
  EmployeeManagement,
};
